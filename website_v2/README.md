# LogLife Website

The marketing and dashboard website for LogLife — chat-native AI journaling that captures your life through voice and conversation.

## Tech Stack

- **Framework**: Next.js 16 with App Router
- **Styling**: Tailwind CSS v4
- **Language**: TypeScript

## Getting Started

```bash
# Install dependencies
pnpm install

# Run the development server
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) to see the website.

## Project Structure

```
website_v2/
├── app/
│   ├── components/     # Reusable UI components
│   ├── contexts/       # React contexts (theme, etc.)
│   ├── hero/           # Homepage hero section
│   ├── features/       # Features page
│   ├── pricing/        # Pricing page
│   ├── dashboard/      # User dashboard
│   ├── blog/           # Blog page and posts
│   ├── login/          # Login page
│   ├── signup/         # Signup page
│   └── api/            # API routes
├── public/             # Static assets (logos, images)
└── ...
```

## Branding

- **Primary Color (Emerald)**: `#10b981` — used for CTAs, accents
- **Background**: Dark slate (`#0a0a0a` / `#0f172a`)
- **Theme**: Dark theme with emerald green accents

LogLife — Your Life, Captured.
